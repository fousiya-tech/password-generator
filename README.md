# password-generator
Python random password generator project

This project generates a random password using Python.

Features:
* Uses random module
* Includes letters, numbers, and symbols
* Generates secure password output
